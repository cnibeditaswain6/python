# WAP to count the number of students with the "A" grade in the following tuple.

grade = ("C","D","A","A","B","B","A")
print("The Number of Students with 'A' Grade : ", grade.count("A"))

# store the above values in a list & sort them from "A" to "D".

list = ["C","D","A","A","B","B","A"]
list.sort()
print(list)